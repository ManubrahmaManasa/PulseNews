<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PulseNews" parent="Theme.MaterialComponents.Light">
        <item name="actionBarStyle">@style/LightActionBar</item>
        <item name="android:textColorPrimary">@color/black</item>
        <item name="popupMenuStyle">@style/CustomPopupMenuStyle</item>

        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="LightActionBar" parent="Widget.AppCompat.Light.ActionBar.Solid">
        <item name="background">@color/white</item>
        <item name="titleTextStyle">@style/LightActionBarTitleText</item>
    </style>

    <style name="LightActionBarTitleText" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="CustomPopupMenuStyle" parent="Widget.AppCompat.PopupMenu">
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">16sp</item> <!-- May not work depending on version -->
    </style>



    <style name="Theme.PulseNews" parent="Base.Theme.PulseNews" />
</resources>